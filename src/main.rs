#[derive(Debug)]
struct Cli {
    private_token: String,
    project_id: String,
    target_branch: String,
    base_branch: String,
    branch: String,
}

fn main() {
    println!("Hello, world!");
}
